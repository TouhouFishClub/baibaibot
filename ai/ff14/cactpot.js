module.exports = function

const PerfectCactpot = (content, callback) => {
  let c = content.trim().toLowerCase().replace(/[\n ]/g, '').split('')
  if(c.length == 9 && Array.from(new Set(c)).length == 5) {

  }

}